---
description: Search the AI dev knowledge base for concepts/playbooks by tag, type, phase, or freetext.
---

Invoke the `kb-search` skill with the user's arguments: $ARGUMENTS

Resolve `$AI_KB_PATH` (or use the plugin's own directory if running inside the KB repo).
