---
description: Process accumulated material in _inbox/ — routes URLs and drops via add-source / distill-concept.
---

Invoke the `triage-inbox` skill. Operate against the KB at `$AI_KB_PATH` (or current repo if inside the KB).
